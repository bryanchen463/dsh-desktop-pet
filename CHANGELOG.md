# 迭代记录

桌宠最初是作为 DSH 动态 Cordis 插件逐版打磨出来的，下面的 `pkg-*` 是运行时的不可变版本包编号。

## pkg-23 · 初版头像桌宠
- 右下角显示一只深色圆润头像（脸、耳朵、嘴巴、高光）。
- 眼睛用 CSS `translate` 偏移追踪鼠标。
- **问题**：事件绑在头像自身区域，鼠标移开后就不再更新，只能盯着右下角。

## pkg-24 · 全屏追踪修复
- 改为监听 `document` 的 `mousemove`，按窗口尺寸归一化坐标。
- 眼睛在整个页面范围内都能跟随鼠标。

## pkg-25 · 只保留眼睛
- 去掉脸部、耳朵、嘴巴，只留一双眼睛。
- 加入睫毛与爱心装饰，两只眼睛高低、旋转不同，更俏皮。

## pkg-26 · 参考图形象（紫色圆球）
- 按参考图改为紫色球体 + 两只白色倾斜椭圆眼睛。
- 加入上下浮动与定时眨眼。

## pkg-27 · 平面化 + 可拖动
- 去掉径向渐变、内阴影和高光，改为纯平面配色。
- 支持按住拖动，拖动时暂停浮动动画并切换抓取光标。
- 眼睛改为按「鼠标相对球心」的方向偏移。

## pkg-28 · 强制正圆
- 锁定 116×116、`aspect-ratio: 1/1`、`flex: 0 0 auto`、`overflow: hidden`。
- **后续发现**：仍会被宿主样式影响，看起来「有点方」。

## pkg-29 · 改用 SVG 绘制
- 球体改为内联 SVG `<circle>`，眼睛改为 SVG 圆角矩形，彻底摆脱 CSS 圆角依赖。
- 去掉 `box-shadow`，避免圆外出现方形阴影；只有圆形本体可点击。

## pkg-30 · 点击眨眼
- 点击圆球连眨两下眼睛；位移小于 5px 才算点击，拖动不会误触。

## pkg-31 · 随机动作
- 每隔 3.2–8 秒随机执行：眨眼 / 四处张望 / 弹跳 / 摇头。
- 「四处张望」在检测到用户正在移动鼠标时会自动跳过，避免和鼠标追踪打架。

## 1.0.0 · 开源整理
- 把 pkg-31 整理为仓库源码：`src/desktop-pet.client.js`（DSH 动态插件）与 `standalone/desktop-pet.js`（无依赖网页版）。
- 网页版补齐 `size` / `color` / `right` / `bottom` / `idleMin` / `idleMax` 配置、`destroy()` 清理与演示页。

## 1.1.0 · 可安装进任意 DSH 部署
- 新增 `profile-plugin/`：out-of-tree 客户端插件包（`package.json` 声明 `dsh.client`，
  `lib/index.js` 为 node 半边，`lib/client.js` 为 `__ModuleLoader__` 懒加载工厂）。
- 客户端半边改用 Cordis 服务注入 `["slots", "timer"]`，样式与定时器分别由 `ctx.effect`、
  `ctx.timeout` 持有，随插件行的生命周期回收。
- README 补上三种引入方式（Web profile 插件 / 动态 Cordis 插件 / 无依赖网页版）与卸载、自定义说明。
- 实测：DSH `0.1.2-rc.1` + Web profile 下复制插件包并在 `cordis.patch.yml` 插入一行，刷新页面即生效。

## 1.1.1 · 安装文档补全
- README 的「在其他 DSH 部署中使用」扩写为完整上手流程：找 profile 目录 → 两种取包方式（复制 / clone）
  → 补丁行写法与缩进 → 重启与刷新 → 生效确认 → 排错表 → 升级与卸载 → 自定义入口。
- 补充说明：拖动位置不做持久化；同一部署要给多个 profile 安装时逐个重复操作。
